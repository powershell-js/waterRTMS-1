const getters = {
    visitedViews: state => state.Layout.visitedViews,
    cachedViews: state => state.Layout.cachedViews,
    menuIndex:state => state.Layout.menuIndex,
    fullscreen:state => state.Layout.fullscreen,
  }
  export default getters
  